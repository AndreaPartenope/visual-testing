const menuButton = "div[class='bm-burger-button']"
const closeMenuButton = "[class='bm-cross-button'] button"
const homeButton = "#home_button"
const newSessionButton = "#new_session"
const sessionListButton = "#session_list"
const carousel = "[class='carousel-inner']"
const inputNameSession = "#nomeSessione"
const submitButton = "#new_session_submit"
const loginForm = "#Login"
const sessionListHeader = "[class='card-header']"
const nextCarouselButton = "a[class='carousel-control-next']"
const previousCarouselButton = "a[class='carousel-control-prev']"
const activeCarouselImg = "div[class='carousel-item active'] img"
const menu = "#menu"

export default {
    menuButton,
    closeMenuButton,
    homeButton,
    newSessionButton,
    sessionListButton,
    carousel,
    inputNameSession,
    submitButton,
    loginForm,
    sessionListHeader,
    nextCarouselButton,
    previousCarouselButton,
    activeCarouselImg,
    menu
}